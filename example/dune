(include_subdirs unqualified)

(env
 (_
  (flags
   (-w +a-27))))

(melange.emit
 (target example)
 (alias example)
 (libraries reshowcase reason-react)
 (module_systems es6)
 (preprocess
  (pps melange.ppx reason-react-ppx)))

(rule
 (targets Demo.re)
 (action
  (run reshowcase generate)))
